load("@rules_rust_mdbook//:defs.bzl", "mdbook", "mdbook_server")

mdbook(
    name = "docs",
    srcs = glob(["src/**/*.md"]),
    book = "//docs:book.toml",
)

mdbook_server(
    name = "server",
    book = ":docs",
)
